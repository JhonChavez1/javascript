let a = 5;
let b = '5';
console.log(a);
console.log(b);

//Operadores de igualdad ==
// Solo compara valores, y hace una conversión del tipo de dato si es necesario
console.log(a == b)

//Operador de igualdad estricta o exacta
// compara el valor y el tipo de dato
console.log(a === b);

// operadores distintos
// compara el valor y lo convierte en un tipo de dato si es necesario
console.log(`${a} != ${b} -> ${a != b}`);

// operador distinto exactto
// compara el valor y el tipo de dato
console.log(`${a} !== ${b} -> ${a !== b}`);

// Operador menor que
console.log(`${a} <${b} -> ${a < b}`);

// Operador mayor que
console.log(`${a} > ${b} -> ${a > b}`);

// Operador menor o igual que
console.log(`${a} <= ${b} -> ${a <= b}`);

// Operador mayor o igual qeu
console.log(`${a} >= ${b} -> ${a >= b}`)

